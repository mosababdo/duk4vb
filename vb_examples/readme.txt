
the with_debug project is currently in early stages of dev 
other two are complete

the debugger project uses a couple activex controls

scintinilla is being used for syntax hilite and loaded through
the scivb_lite.ocx. I also use the sandsprite subclass component 
to help extend it a little further.

